# smart-file-organizer
A Python-based desktop and CLI tool that automatically organizes files by extension and filename patterns. Includes a GUI (Tkinter) version and safe copy-only mode for non-destructive file management.
